# Local dev credentials. Loaded by the sidecar when started outside VS Code
# (e.g. via `npm run dev:sidecar`). The VS Code extension uses SecretStorage
# instead and these values are NOT consulted in that case.
#
# Copy this file to `.env` and fill in your keys. Never commit `.env`.

SUPABASE_URL=https://mgdbizkfgplhlfprajmd.supabase.co
SUPABASE_SERVICE_KEY=replace-me-with-sb_secret_...-or-service-role-jwt
OPENROUTER_API_KEY=replace-me-with-sk-or-v1-...
